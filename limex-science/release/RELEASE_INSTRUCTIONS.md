# RC4 release instructions

1. Preserve this RC as immutable evidence after manifest creation.
2. Verify `RESEARCH_PAYLOAD_BINDING.json` against the immutable `goldbach-v1.8.767` tag and preserve `proof_status = NO_PROOF`.
3. Preserve the owner-selected licence and privacy boundaries without presenting them as legal or privacy certifications.
4. For any sponsored-compute pilot, implement and independently attest every `A1` through `A14` service gate. Structural packet validation is not accreditation.
5. Create a new successor tree; never mutate a frozen RC.
6. Freeze RC4 once, then create an external release envelope binding package root, ZIP, commit, tag, repository, post-freeze independent audit and digest-bound executive confirmation.
7. Publish only after the envelope is complete; then verify remote commit, tag, release page and visible assets.

Until all applicable steps pass, keep `external_effect = DENY`.
